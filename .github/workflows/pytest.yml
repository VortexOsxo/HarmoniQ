name: Backend Pytest Tests

on:
    push:
        branches: [main, develop]
    pull_request:
    workflow_dispatch:

jobs:
    tests:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                python-version: '3.11'

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Upgrade pip
              run: python -m pip install --upgrade pip

            - name: Install HarmoniQ (dev mode)
              working-directory: harmoniQ
              run: |
                  pip install -e .[dev]

            - name: Run tests
              working-directory: harmoniQ
              run: pytest
